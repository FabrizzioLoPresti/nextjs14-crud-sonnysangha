import TodosList from '@/components/todos-list';
import React from 'react';

type Props = {};

export default function TodosPage({}: Props) {
  return (
    <section className="mx-auto max-w-7xl">
      <p>There is where the todos will be listed</p>
    </section>
  );
}
